<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base de la app -->
    <style name="Theme.SummaryNews" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Colores principales AppBar -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Estilo de la AppBar y nuevo -->
        <item name="actionBarTheme">@style/Theme.SummaryNews.AppBarOverlay</item>

        <!-- Color de la status bar -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Otros -->
        <!-- <item name="android:textViewStyle">@color/text_color</item> -->
    </style>

    <!-- Tema sin ActionBar -->
    <style name="Theme.SummaryNews.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
